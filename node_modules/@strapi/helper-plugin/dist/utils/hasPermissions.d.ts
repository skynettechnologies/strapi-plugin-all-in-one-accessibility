import type { domain } from '@strapi/permissions';
import type { GenericAbortSignal } from 'axios';
type Permission = domain.permission.Permission;
declare const findMatchingPermissions: (userPermissions: Permission[], permissions: Permission[]) => domain.permission.Permission[];
declare const formatPermissionsForRequest: (permissions: Permission[]) => {}[];
/**
 * This should fail if there are no permissions or if there are permissions but no conditions
 */
declare const shouldCheckPermissions: (permissions: Permission[]) => boolean;
declare const hasPermissions: (userPermissions: Permission[], permissions: Permission[], signal?: GenericAbortSignal) => Promise<boolean>;
export { hasPermissions, findMatchingPermissions, formatPermissionsForRequest, shouldCheckPermissions, };
