import styled from 'styled-components';
import { avatarSize } from './constants.mjs';
import { Flex } from '../Flex/Flex.mjs';

const AvatarGroup = styled(Flex) `
  & > * + * {
    margin-left: -${avatarSize / 2}px;
  }
`;

export { AvatarGroup };
