import { createContext, useContext } from 'react';

const FieldContext = createContext({ id: '', required: false });
const useField = () => useContext(FieldContext);

export { FieldContext, useField };
