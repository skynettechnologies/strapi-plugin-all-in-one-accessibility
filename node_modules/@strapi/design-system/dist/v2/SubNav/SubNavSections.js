'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const React = require('react');
const Box = require('../../Box/Box.js');
const Flex = require('../../Flex/Flex.js');

const SubNavSections = ({ children, spacing = 2, horizontal = false, ...props }) => {
    return (jsxRuntime.jsx(Box.Box, { paddingTop: 2, paddingBottom: 4, children: jsxRuntime.jsx(Flex.Flex, { as: "ol", gap: spacing, direction: horizontal ? 'row' : 'column', alignItems: horizontal ? 'center' : 'stretch', ...props, children: React.Children.map(children, (child, index) => {
                /* eslint-disable react/no-array-index-key */
                return jsxRuntime.jsx("li", { children: child }, index);
            }) }) }));
};

exports.SubNavSections = SubNavSections;
